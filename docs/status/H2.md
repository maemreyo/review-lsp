# H2 status — TypeScript semantic vertical slice

State: CLOSED for the dependency-free A/B fixture profile.

Acceptance: live checkout is commit B while a retained session for commit A returns A semantics. A exports `string`; B exports `number`. Hover differs accordingly. Definition from A is classified and digest-bound to A's `src/value.ts`.

Also covered:

- Unicode before the hover position (UTF-16 code-unit position).
- Parallel A/B sessions do not share document/result state.
- Candidate mutation invalidates a query before a usable success receipt.
- Receipt mutation is rejected by the independent validator.
- Definition URI is classified as candidate/toolchain/unbound.

This does not close H1 for dependency-bearing projects.

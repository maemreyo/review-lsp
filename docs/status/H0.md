# H0 status

State: CLOSED for the first TypeScript vertical slice.

## Frozen decisions

- Core standalone from OCR/Pi/Workbench.
- Driver: thin stdio LSP client using VS Code protocol libraries.
- Candidate source: Git object database, never live checkout bytes.
- Native isolation: TRUSTED_LOCAL only.
- First operations: hover and definition.
- First language/profile: TypeScript.
- Receipt: content-addressed JSON with source/environment/profile/session/document/request/result binding.
- Request timeout uses a monotonic timer; wall clock is observation metadata only.

## Known H0 gaps intentionally deferred

- Container isolation for untrusted PRs.
- Dependency installation/preparation workflow for arbitrary repositories.
- MCP interface (H3).
- OCR/Pi/Workbench consumer adapters (H4).
- References/symbols/diagnostics (post-v0.1/H7).

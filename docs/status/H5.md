# H5 status — 0.1.0-alpha.1

State: RELEASE CANDIDATE VERIFIED; NPM PUBLICATION PENDING.

Target: a public alpha that an external user can install and run without Zamery/Workbench source.

Release-input hardening authority commit:

```text
4b16609a90033c438449efef89974ba303ac0fb2
```

GitHub CI evidence candidate:

```text
62aec8bcb2d73815c3bf760cc0de04c879a69ca5
```

Later status-only commits may advance HEAD without changing the reviewed release implementation.

## Local gates completed

- Versioned package metadata: 0.1.0-alpha.1.
- Public package file allowlist.
- Apache-2.0 LICENSE + NOTICE.
- Direct runtime dependency license evidence report.
- Bundled CLI entrypoint plus content-addressed artifact manifest.
- Clean-install npm tarball smoke in an isolated consumer.
- A/B semantic demo remains candidate-bound after live checkout moves.
- MCP stdio smoke.
- LSP timeout/crash/in-flight-close fault injection.
- Local macOS arm64 / Node 24.19.0 full check.
- Observational benchmark recorded.
- H5 self-review completed:
  - historical candidate `249e222d...`: 21/21 reviewable entries accounted;
  - three release-hardening findings preserved on that candidate;
  - successor `4b16609a...`: 2/2 reviewable entries, 0 findings;
  - all three historical findings moved to ADDRESSED with successor-review provenance.
- Release verifier inputs hardened:
  - GitHub Actions pinned to immutable commit SHAs;
  - CI jobs have explicit timeouts and workflow concurrency cancellation;
  - Docker Node base image pinned to an exact manifest digest.
- Exact-clean pnpm check PASS on local release candidate.
- Pinned GitHub action commits and pinned Docker base digest resolve successfully.

## Remote CI gate — PASSED

GitHub Actions workflow run `35377641822` on `62aec8bcb2d73815c3bf760cc0de04c879a69ca5` completed SUCCESS.

Passed jobs:

- compatibility — macOS 14 / Node 22.19.0
- compatibility — macOS 14 / Node 24.19.0
- compatibility — Ubuntu 24.04 / Node 22.19.0
- compatibility — Ubuntu 24.04 / Node 24.19.0
- linux-container — Docker build + CONTAINER_READ_ONLY smoke
- benchmark

This closes the remote compatibility/container gate for the stated alpha matrix.

## Remaining publication gates

1. Authenticate to npm.
2. Confirm `review-lsp` remains unclaimed immediately before publication.
3. Publish `review-lsp@0.1.0-alpha.1` with the alpha dist-tag.
4. Verify the package from the public registry using a clean consumer install.
5. Create the Git tag and GitHub Release only after registry verification succeeds.

## Claim ceiling

The alpha does not claim dependency-snapshot completeness, arbitrary repository support, semantic correctness of the language server, Windows support, JavaScript support, dirty-worktree candidates, remote MCP, signed receipts, or hermetic protection from a malicious container host administrator.

Dependency-bearing projects remain `environment_binding=PARTIAL` until dependency snapshot preparation/admission exists.

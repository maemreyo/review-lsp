# Compatibility evidence — 0.1.0-alpha.6

This document defines the compatibility dimensions required for the exact Alpha.6 release commit. Publication is blocked unless the required local gates and GitHub Actions rows are green on that exact candidate.

| Platform | Node | Native check | Package install | Linux container | Required state |
| --- | --- | --- | --- | --- |
| macOS arm64 (local) | 24.19.0 | PASS required | PASS required | N/A | observed locally |
| macOS 14 (GitHub Actions) | 22.19.0 | required | through `pnpm check` | N/A | PASS before publish |
| macOS 14 (GitHub Actions) | 24.19.0 | required | through `pnpm check` | N/A | PASS before publish |
| Ubuntu 24.04 (GitHub Actions) | 22.19.0 | required | through `pnpm check` | not in this matrix cell | PASS before publish |
| Ubuntu 24.04 (GitHub Actions) | 24.19.0 | required | through `pnpm check` | dedicated job | PASS before publish |

Additional required jobs:

- benchmark;
- linux-container;
- overall workflow conclusion SUCCESS.

## Semantic compatibility boundary

Alpha.6 requires both admitted engine generations to preserve the ownership/routing contract:

- TypeScript 6 compatibility profile;
- TypeScript 7 native profile.

Required ownership cases include:

- explicit `files` ownership;
- `include` / `exclude` ownership;
- inherited membership through admitted relative `extends`;
- effective `allowJs` include eligibility, including `jsconfig.json` default behavior;
- nested monorepo routing where path proximity would select the wrong config;
- root/child or sibling overlap producing `AMBIGUOUS` rather than an arbitrary owner;
- solution-style roots with `files: []` and project references not stealing child ownership.

The exact release candidate must also pass:

- ownership/adversarial unit tests;
- full TypeScript 6/7 semantic conformance;
- typecheck and build;
- full `pnpm check`;
- package/MCP/cache-crash smokes;
- Linux `CONTAINER_READ_ONLY` smoke;
- benchmark;
- real pinned-provider Zamery Workbench dogfood;
- independent implementation and release-prep reviews with no unresolved blocking findings.

## Consumer evidence

Workbench dogfood verification:

- run: `run_ea496e78-b288-4cc7-9d91-4a5e920a2cc1`;
- state: PASS;
- test: nested document routes to evidence-proven root project instead of nearer non-owning child config;
- external receipt: source/environment VERIFIED and exact resolving-project identity present;
- candidate A semantics remain stable after live checkout advances to B.

## Non-claims

A compatibility PASS does not assert:

- full TypeScript default file-discovery semantics;
- package-based `extends`;
- universal TypeScript build-mode/project-graph completeness;
- arbitrary package `exports/imports` or workspace routing;
- project-wide build correctness;
- semantic correctness of LSP answers.

A post-publication status update may add the concrete GitHub workflow run ID and public-registry smoke identifier without changing the package bytes that were published.

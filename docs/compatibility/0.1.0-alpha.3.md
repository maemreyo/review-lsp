# Compatibility evidence — 0.1.0-alpha.3

This document defines the compatibility dimensions required for the exact alpha.3 release commit. Publication is blocked unless the GitHub Actions workflow is green across all required rows.

| Platform | Node | Native check | Package install | Linux container | Required state |
| --- | --- | --- | --- | --- | --- |
| macOS arm64 (local) | 24.19.0 | PASS required | PASS required | N/A | observed locally |
| macOS 14 (GitHub Actions) | 22.19.0 | required | through pnpm check | N/A | PASS before publish |
| macOS 14 (GitHub Actions) | 24.19.0 | required | through pnpm check | N/A | PASS before publish |
| Ubuntu 24.04 (GitHub Actions) | 22.19.0 | required | through pnpm check | not in this matrix cell | PASS before publish |
| Ubuntu 24.04 (GitHub Actions) | 24.19.0 | required | through pnpm check | dedicated job | PASS before publish |

Additional required jobs:

- benchmark;
- linux-container;
- overall workflow conclusion SUCCESS.

Alpha.3 additionally requires a real-repository dependency-acquisition dogfood where pnpm lockfile file: entries reference candidate-contained .tgz files. The acquired graph must be SATISFIED, use FROZEN lockfile policy and IGNORE_SCRIPTS, and bind the tarball bytes into the dependency input identity.

A post-publication status update may add the concrete GitHub workflow run ID and public-registry smoke identifier without changing the package bytes that were published.

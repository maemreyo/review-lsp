# Compatibility evidence — 0.1.0-alpha.5

This document defines the compatibility dimensions required for the exact Alpha.5 release commit. Publication is blocked unless the GitHub Actions workflow is green across all required rows.

| Platform | Node | Native check | Package install | Linux container | Required state |
| --- | --- | --- | --- | --- |
| macOS arm64 (local) | 24.19.0 | PASS required | PASS required | N/A | observed locally |
| macOS 14 (GitHub Actions) | 22.19.0 | required | through `pnpm check` | N/A | PASS before publish |
| macOS 14 (GitHub Actions) | 24.19.0 | required | through `pnpm check` | N/A | PASS before publish |
| Ubuntu 24.04 (GitHub Actions) | 22.19.0 | required | through `pnpm check` | not in this matrix cell | PASS before publish |
| Ubuntu 24.04 (GitHub Actions) | 24.19.0 | required | through `pnpm check` | dedicated job | PASS before publish |

Additional required jobs:

- benchmark;
- linux-container;
- overall workflow conclusion SUCCESS.

Alpha.5 semantic compatibility requires both admitted engine generations:

- TypeScript 6 compatibility profile: supported candidate-contained project-reference graph plus existing hover/definition/references/document-diagnostics behavior;
- TypeScript 7 native profile: the same admitted graph boundary with exact-project toolchain evidence kept distinct from environment reproducibility.

The exact release candidate must also pass:

- candidate-bound project-reference unit and TypeScript 6/7 acceptance tests;
- package/MCP/cache-crash smokes;
- Linux `CONTAINER_READ_ONLY` smoke;
- benchmark;
- real pinned-provider Zamery Workbench dogfood for the existing environment binding/hash/limitations contract;
- independent code review with no unresolved blocking findings.

Project-reference compatibility is intentionally a strict admitted subset. A PASS does not assert universal TypeScript build-mode graph completeness, exact `files`/`include` ownership, arbitrary config-chain resolution, or project-wide build correctness.

A post-publication status update may add the concrete GitHub workflow run ID and public-registry smoke identifier without changing the package bytes that were published.

# Compatibility evidence — 0.1.0-alpha.4

This document defines the compatibility dimensions required for the exact Alpha.4 release commit. Publication is blocked unless the GitHub Actions workflow is green across all required rows.

| Platform | Node | Native check | Package install | Linux container | Required state |
| --- | --- | --- | --- | --- | --- |
| macOS arm64 (local) | 24.19.0 | PASS required | PASS required | N/A | observed locally |
| macOS 14 (GitHub Actions) | 22.19.0 | required | through `pnpm check` | N/A | PASS before publish |
| macOS 14 (GitHub Actions) | 24.19.0 | required | through `pnpm check` | N/A | PASS before publish |
| Ubuntu 24.04 (GitHub Actions) | 22.19.0 | required | through `pnpm check` | not in this matrix cell | PASS before publish |
| Ubuntu 24.04 (GitHub Actions) | 24.19.0 | required | through `pnpm check` | dedicated job | PASS before publish |

Additional required jobs:

- benchmark;
- linux-container;
- overall workflow conclusion SUCCESS.

Alpha.4 semantic compatibility requires both admitted engine generations:

- TypeScript 6 compatibility profile: references plus deterministic legacy document diagnostics through `typescript.tsserverRequest` using `syntacticDiagnosticsSync`, `semanticDiagnosticsSync`, and `suggestionDiagnosticsSync`;
- TypeScript 7 native profile: references plus standard `textDocument/diagnostic` when `diagnosticProvider` is advertised.

The exact release candidate must also pass:

- candidate-bound references and diagnostics unit/acceptance tests;
- package/MCP/cache-crash smokes;
- Linux `CONTAINER_READ_ONLY` diagnostics without synthetic coordinates;
- real pinned-provider Zamery Workbench dogfood for references and diagnostics;
- independent code review with no unresolved blocking findings.

Diagnostics compatibility is document-scoped. A PASS does not assert project-wide build correctness.

A post-publication status update may add the concrete GitHub workflow run ID and public-registry smoke identifier without changing the package bytes that were published.
